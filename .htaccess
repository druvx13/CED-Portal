RewriteEngine On
RewriteBase /

# Serve existing files in public/ directly
RewriteCond %{DOCUMENT_ROOT}/public/$1 -f
RewriteRule ^(.*)$ public/$1 [L]

# Redirect everything else to public/index.php
RewriteRule ^(.*)$ public/index.php [QSA,L]
